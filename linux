%g          按照实际结果输出位数
%p          打印地址
gets()     输入字符串
puts(参数)　　输入字符串
1.计算机网络
2.Linux网络编程
3.Unix网络编程第一卷
4.Unix网络编程卷2(书中第四部分是重点)
5.Linux高性能服务器编程





SYN   ＝１    同步
ＡＣＫ　＝１　　确认
ｓｅｑ        序列号缩写
对方发送的每一个数据都得收到，必须保证收到的数据有序

lsof -i:(port)　　显示某个端口的进程
netstat -a | grep (port)  显示端口详细状态信息


每个socket 都有自己的读和写缓冲区 

进程退出方式　
    1.exit 　exit(0) ：表示正常退出
    2. _exit  :直接退出

fork():创建一个子进程.fork()之前的代码父子进程共享,

mmap [memory map] :向内核申请共享内存
0 1 2 是三个文件描述符:标准输入　1:标准输出 2:标准错误
原子操作(原子指令):最小指令,即这条指令不管时间片到不到都必须执行完该指令才会释放cpu
 







Question:

内存存取数据比硬盘快多少    
什么是动态库　　静态库
指针的0 和null 有什么区别
回调函数　　　钩子函数　 
void *( * sta)(void *)
～_～
－_－!




